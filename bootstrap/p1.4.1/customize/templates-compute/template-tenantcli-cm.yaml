apiVersion: v1
kind: ConfigMap
metadata:
  name: tenantcli-cm
  namespace: hpe-templates-compute
data:
  pre-startup.sh: |
    #!/usr/bin/env bash

  post-startup.sh: |
    #!/usr/bin/env bash
    
  example.conf: |
    # put conf files here
