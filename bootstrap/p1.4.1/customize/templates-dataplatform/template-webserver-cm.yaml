apiVersion: v1
kind: ConfigMap
metadata:
  name: webserver-cm
  namespace: hpe-templates-data
data:
  example.conf: |
    # put conf files here

  pre-startup.sh: |
    #!/usr/bin/env bash

  post-startup.sh: |
    #!/usr/bin/env bash
