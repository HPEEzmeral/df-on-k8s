apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-cm
  namespace: hpe-templates-data
data:
  pre-startup.sh: |
    #!/usr/bin/env bash

  post-startup.sh: |
    #!/usr/bin/env bash
    
  example.conf: |
    # put conf files here
